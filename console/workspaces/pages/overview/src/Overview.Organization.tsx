import React from 'react';
import { ProjectList } from './ProjectList/ProjectList';

export const OverviewOrganization: React.FC = () => {
  return (
    <ProjectList />
  );
};

export default OverviewOrganization;
